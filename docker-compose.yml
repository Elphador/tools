version: "3.9"
services:
  workedsdrdisk:
    build: .
    environment:
      - API_ID=${API_ID}
      - API_HASH=${HASH}
      - TOKEN=${TOKEN}
      - DB_NAME=${DB_NAME}
      - DB_URL=${DB_URL}
      - CHANNEL=${CHANNEL}
      - ADMIN=${ADMIN}
